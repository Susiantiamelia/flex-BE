const {User} = require("../models")
const jwt = require("../helpers/jwt")

class Auth {

    static async authentication(req, res, next){
        try {

        } catch (err) {
        }
    }
}

module.exports = Auth